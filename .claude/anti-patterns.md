## Anti-patterns

- Do not:
  - fetch internal API from server component
  - use spread on typed arrays
  - block UI on API failure
  - mix UI + data fetching logic

- Avoid:
  - large components
  - duplicated API logic